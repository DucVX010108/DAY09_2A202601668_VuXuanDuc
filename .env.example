# Copy this file to .env and replace the placeholder locally.
# Never commit the real .env file or an API key.
OPENAI_API_KEY=your_openai_api_key_here

# The lab requires the chosen model name in source code and metadata.json,
# not in this environment file.
