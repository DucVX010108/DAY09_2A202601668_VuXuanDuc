"""Declared execution model for the grading pipeline.

The decision path is deterministic and uses no generative model.  Keeping this
declaration in source and metadata satisfies the submission audit requirement.
"""

MODEL_NAME = "deterministic-rule-based"
MODEL_PARAMETER_SIZE_BILLION = 0
