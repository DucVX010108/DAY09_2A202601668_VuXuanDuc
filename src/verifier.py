"""Independent K4 output verifier. Owned by Member 5."""


def verify_output(output: dict, facts: dict, repository: object) -> list[str]:
    """Return an empty list only when the output is fully valid.

    TODO(M5): validate schema, raw IDs, calculations, policy and limits.
    """

    raise NotImplementedError

